# Accept current Node when it satisfies a reasonable minimum (avoids EBADENGINE for deps)
engine-strict=false
# Only show errors (hides deprecation and engine warnings from transitive deps)
loglevel=error
